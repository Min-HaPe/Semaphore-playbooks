
---
  hosts: Ubuntu

  tasks:
    - name: Ping hosts
      ping:
      when: 
        - ping == "Pong"
      notify: Message


  handlers:
    - name: Message
      debug:
        msg: "Host is alive!"

...

